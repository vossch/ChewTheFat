import SwiftUI

enum AppColor {
    static let backgroundPrimary = Color("colorBackgroundPrimary")
    static let backgroundSecondary = Color("colorBackgroundSecondary")
    static let surface = Color("colorSurface")
    static let textPrimary = Color("colorTextPrimary")
    static let textSecondary = Color("colorTextSecondary")
    static let accent = Color("colorAccent")
    static let success = Color("colorSuccess")
    static let warning = Color("colorWarning")
    static let error = Color("colorError")
    static let border = Color("colorBorder")
}
